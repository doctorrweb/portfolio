/*

const { env } = process

const config = {
    nodeEnv: env.NODE_ENV || 'development',
    logStars: message => {
        console.info('*********')
        console.info(message)
        console.info('*********')
    },
    port: 'set the port',
    DB: 'set the production DB',
    devDB: 'set the devlopment database',
    testDB: 'set the test database'
}

module.exports = config

*/